{
  "mcpServers": {
    "markitdown": {
      "command": "docker",
      "args": [
        "compose",
        "--profile", "mcp",
        "-f", "/ABSOLUTE/PATH/TO/docs-for-ai/docker-compose.yml",
        "run", "--rm", "-T", "mcp"
      ],
      "env": {}
    }
  }
}
